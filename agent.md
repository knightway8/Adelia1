# Adelia AI guidance

Read [AGENTS.md](AGENTS.md), the canonical project instructions used by AI coding tools.

It requires the latest stable PHP with no older-PHP compatibility requirement, supports SQLite 3 and flat-file only, and makes improving flat-file reliability and consistency a central goal. The flat-file system is attempting to be the best it can be for a lightweight board.
